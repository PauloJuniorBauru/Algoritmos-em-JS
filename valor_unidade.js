/*
DESAFIO
Desenvolva um algoritmo que solicite um valor de até 3 digitos, após a entrada de dados, mostrar o valor da unidade. Exemplo: 15, o resultado será 5.
*/

const valor = 38; //Valor de Entrada.

let unidade = valor % 10; //Para conseguir a unidade do número use o operador % (Modulo).

console.log(unidade);
